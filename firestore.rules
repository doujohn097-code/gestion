rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open for testing only - do not use in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
